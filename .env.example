# Documentation only: application does not automatically load this file.
# Generate a random secret of >=32 characters; no preset production secret.
SHOP_SECRET_KEY=
SHOP_DATABASE=runtime/shop.sqlite3
# lab.run uses its own local configuration and generates runtime/session.key.
# Production host allowlist, TLS, log paths and service wiring are stage-2 work.
